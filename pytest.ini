[pytest]
addopts =
    --doctest-modules
    --doctest-continue-on-failure
    --junitxml=junit/test-results.xml
    --cov=meeteval
    --cov-report=xml
    --cov-report=html
    --ignore=meeteval/der/dscore_repo/scorelib/

testpaths =
    tests
    meeteval
